name: smart_diabetes_app
description: Smart Diabetes Monitoring and Prediction System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  http: ^1.1.0
  shared_preferences: ^2.2.1
  fl_chart: ^0.65.0
  lucide_icons: ^0.257.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
